#TRK-4F41B1FD
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-jtmgmt-svc
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "jtmgmt.labels.app" . }}
    component: jtmgmt
    name: {{ .Release.Name }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "jtmgmt.labels.app" . }}
    component: jtmgmt
    name: {{ .Release.Name }}
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP

